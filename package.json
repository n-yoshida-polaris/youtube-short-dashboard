{
  "name": "youtube-dashboard",
  "version": "1.0.0",
  "description": "YouTube Shorts multi-channel management dashboard",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "collect": "npm run collect --prefix backend",
    "add-channel": "npm run add-channel --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
